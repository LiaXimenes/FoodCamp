function comida(numero){
    const naoverde = document.querySelector(".verde")
        if (naoverde != null){    
           naoverde.classList.remove("verde");
        }
    const selecionado = "." + numero;
    const comida = document.querySelector(selecionado);
    comida.classList.add("verde");
  
}

function bebida(numero){
    const naoverde = document.querySelector(".verde")
        if (naoverde != null){    
           naoverde.classList.remove("verde");
        }
    const selecionado = "." + numero;
    const comida = document.querySelector(selecionado);
    comida.classList.add("verde");
  
}


function doce(numero){
    const naoverde = document.querySelector(".verde")
        if (naoverde != null){    
           naoverde.classList.remove("verde");
        }
    const selecionado = "." + numero;
    const comida = document.querySelector(selecionado);
    comida.classList.add("verde");
  
}





// function botaofinal(){

//     if (ao selecionar os 3 pratos o botão tem que ser ativado){
        
//         botaofim.classList.add("finalizacao-verde")
//      innerHTML("Fechar Pedido")

//     }
// }